#include "stdafx.h"

#include "eclipse.h"

int main(int argc, char **argv)
{
	/*Colour col = {128,128,128,128};
	const Colour *fetched = 0;

	colours_load("test",&col);
	fetched = colours_get("bleh");*/

	/*baily_initialize();

	baily_setOptions(0,1);

	baily_setResolution(0,800,600);

	baily_start();

	baily_clean();*/

	

	/*
	Mix_Music *m = 0;
	m = Mix_LoadMUS("test.flac");

	Mix_PlayMusic(m,0);

	while(Mix_PlayingMusic());

	Mix_HaltChannel(-1);

	Mix_CloseAudio();*/

	return 0;
}
